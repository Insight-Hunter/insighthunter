import type { IntakeAnswers } from "../../types/formation"; export default function MarketingStep({ values, onChange }: { values: IntakeAnswers; onChange: (next: IntakeAnswers) => void }) { return <section className="panel"><h3>Marketing plan</h3><p>Describe launch channels and budget expectations.</p><label>Marketing plan<textarea value={values.marketingPlan} onChange={(e) => onChange({ ...values, marketingPlan: e.currentTarget.value })} placeholder="SEO, referrals, outbound, paid search" /></label></section>; }
