import type { IntakeAnswers } from "../../types/formation"; export default function FinancingStep({ values, onChange }: { values: IntakeAnswers; onChange: (next: IntakeAnswers) => void }) { return <section className="panel"><h3>Financing approach</h3><p>Outline startup capital and banking assumptions.</p><label>Funding plan<textarea value={values.fundingPlan} onChange={(e) => onChange({ ...values, fundingPlan: e.currentTarget.value })} placeholder="Owner funded, business bank account, credit card" /></label></section>; }
