import type { IntakeAnswers } from "../../types/formation"; export default function ConceptStep({ values, onChange }: { values: IntakeAnswers; onChange: (next: IntakeAnswers) => void }) { return <section className="panel"><h3>Describe the business concept</h3><p>Capture what the business does and who it serves.</p><label>Concept<textarea value={values.concept} onChange={(e) => onChange({ ...values, concept: e.currentTarget.value })} placeholder="AI bookkeeping for small service businesses" /></label></section>; }
