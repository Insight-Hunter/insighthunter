import type { IntakeAnswers } from "../../types/formation"; export default function WebDesignStep({ values, onChange }: { values: IntakeAnswers; onChange: (next: IntakeAnswers) => void }) { return <section className="panel"><h3>Web, domain, and email</h3><p>Track domain, DNS, and business email needs.</p><label>Website needs<textarea value={values.websiteNeeds} onChange={(e) => onChange({ ...values, websiteNeeds: e.currentTarget.value })} placeholder="Domain, email, landing page, DNS setup" /></label></section>; }
