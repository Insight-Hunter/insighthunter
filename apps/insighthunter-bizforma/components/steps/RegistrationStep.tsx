import type { IntakeAnswers } from "../../types/formation"; export default function RegistrationStep({ values, onChange }: { values: IntakeAnswers; onChange: (next: IntakeAnswers) => void }) { return <section className="panel"><h3>Pick formation state</h3><p>Set the registration state and ownership structure.</p><label>State code<input value={values.stateCode} onChange={(e) => onChange({ ...values, stateCode: e.currentTarget.value.toUpperCase() })} placeholder="GA" /></label><label>Owners<input value={values.owners} onChange={(e) => onChange({ ...values, owners: e.currentTarget.value })} placeholder="1 founder, 1 member" /></label></section>; }
