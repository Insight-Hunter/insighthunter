import type { IntakeAnswers } from "../../types/formation"; export default function AccountingStep({ values, onChange }: { values: IntakeAnswers; onChange: (next: IntakeAnswers) => void }) { return <section className="panel"><h3>Accounting setup</h3><p>Define the basic finance stack and recordkeeping plan.</p><label>Accounting stack<textarea value={values.fundingPlan} onChange={(e) => onChange({ ...values, fundingPlan: e.currentTarget.value })} placeholder="Accounting software, CPA review, monthly close" /></label></section>; }
