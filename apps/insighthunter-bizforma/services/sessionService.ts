import type { AuthContext, Env } from "../types/env"; export async function createSession(env: Env, user: AuthContext) { const id = crypto.randomUUID(); await env.SESSIONS.put(`session:${id}`, JSON.stringify({ user }), { expirationTtl: 604800 }); return id; } export async function getSession(env: Env, sessionId: string) { const raw = await env.SESSIONS.get(`session:${sessionId}`); return raw ? JSON.parse(raw) as { user: AuthContext } : null; } export async function deleteSession(env: Env, sessionId: string) { await env.SESSIONS.delete(`session:${sessionId}`); }
