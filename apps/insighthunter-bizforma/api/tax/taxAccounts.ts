import { Hono } from "hono"; import * as taxAccountService from "../../services/taxAccountService"; export const taxAccountsApi = new Hono(); taxAccountsApi.get("/", (c) => c.json({ tasks: taxAccountService.getTaxTasks() }));
