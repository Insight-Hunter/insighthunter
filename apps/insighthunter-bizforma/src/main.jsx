import React from “react”;
import ReactDOM from “react-dom/client”;
import BizForma from “./index.jsx”;

ReactDOM.createRoot(document.getElementById(“root”)).render(
<React.StrictMode>
<BizForma />
</React.StrictMode>
);
