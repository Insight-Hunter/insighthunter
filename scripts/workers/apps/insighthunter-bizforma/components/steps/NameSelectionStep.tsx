import type { IntakeAnswers } from "../../types/formation"; export default function NameSelectionStep({ values, onChange }: { values: IntakeAnswers; onChange: (next: IntakeAnswers) => void }) { return <section className="panel"><h3>Choose a business name</h3><p>Set the working business name used for formation planning.</p><label>Business name<input value={values.businessName} onChange={(e) => onChange({ ...values, businessName: e.currentTarget.value })} placeholder="Insight Hunter BizForma LLC" /></label></section>; }
