import type { IntakeAnswers } from "../../types/formation"; export default function ComplianceStep({ values, onChange }: { values: IntakeAnswers; onChange: (next: IntakeAnswers) => void }) { return <section className="panel"><h3>Compliance planning</h3><p>Document your annual filing and licensing assumptions.</p><label>Compliance notes<textarea value={values.marketingPlan} onChange={(e) => onChange({ ...values, marketingPlan: e.currentTarget.value })} placeholder="Annual registration, BOI tracking, city license" /></label></section>; }
