import type { IntakeAnswers } from "../../types/formation"; export default function EntityTypeStep({ values, onChange }: { values: IntakeAnswers; onChange: (next: IntakeAnswers) => void }) { return <section className="panel"><h3>Select entity type</h3><p>Start with the recommended structure and adjust if needed.</p><label>Entity type<select value={values.entityType} onChange={(e) => onChange({ ...values, entityType: e.currentTarget.value })}><option>LLC</option><option>S-Corp</option><option>C-Corp</option><option>Partnership</option></select></label></section>; }
