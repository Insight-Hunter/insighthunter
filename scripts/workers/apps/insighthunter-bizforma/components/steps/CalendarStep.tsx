import type { IntakeAnswers } from "../../types/formation"; export default function CalendarStep({ values, onChange }: { values: IntakeAnswers; onChange: (next: IntakeAnswers) => void }) { return <section className="panel"><h3>Compliance calendar</h3><p>Review the outcome and confirm the recurring obligation model.</p><div className="review-grid"><div><strong>Concept:</strong> {values.concept || "—"}</div><div><strong>Name:</strong> {values.businessName || "—"}</div><div><strong>Entity:</strong> {values.entityType}</div><div><strong>State:</strong> {values.stateCode}</div></div></section>; }
