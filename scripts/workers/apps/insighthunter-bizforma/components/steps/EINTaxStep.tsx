import type { IntakeAnswers } from "../../types/formation"; export default function EINTaxStep({ values, onChange }: { values: IntakeAnswers; onChange: (next: IntakeAnswers) => void }) { return <section className="panel"><h3>EIN and tax profile</h3><p>Capture hiring intent for payroll and tax setup planning.</p><label>Payroll intent<input value={values.payrollIntent} onChange={(e) => onChange({ ...values, payrollIntent: e.currentTarget.value })} placeholder="Hire in 90 days" /></label></section>; }
